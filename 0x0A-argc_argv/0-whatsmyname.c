#include <stdio.h>

/**
 * main - Entry point
 * @argc: Number of command line arguments
 * @argv: Array of arguments
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
