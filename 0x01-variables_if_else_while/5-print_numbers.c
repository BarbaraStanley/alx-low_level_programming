#include <stdio.h>
/**
 * main - Entry point, print all single digit numbers of base 10 
 *
 * Return: always 0
 */
int main(void)
{
	int i;

	while (i <= 10)
	{
		printf("%d\n", i);
		i++;
	}

	return (0);
}

