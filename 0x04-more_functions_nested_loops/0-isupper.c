#include "main.h"

/**
 * _isupper - Function that checks for uppercase character
 * @c: integer
 * Return: 0
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (0);
	}
	else
	{
		return (0);
	}
}
